<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= book.title %> - Edit Book</title>
    <link rel="stylesheet" href="/editing.css" />
    <link rel="icon" href="/images.jpeg" type="image/jpeg" />
  </head>
  <body>
    <div id="edit-book-container">
      <h1>Edit Book: <%= book.title %></h1>

      <form
        action="/update/<%= book.id %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="form-group">
          <label for="title">Title:</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= book.title %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="read_date">Read Date (optional):</label>
          <input
            type="date"
            id="read_date"
            name="read_date"
            value="<%= book.read_date %>"
          />
        </div>

        <div class="form-group">
          <label for="rating">Rating (1-10):</label>
          <input
            type="number"
            id="rating"
            name="rating"
            min="1"
            max="10"
            value="<%= book.book_rating %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="image">Upload a new cover image (optional):</label>
          <input type="file" id="image" name="image" />
        </div>

        <% if (book.image_path) { %>
        <div class="form-group">
          <strong>Current Image:</strong>
          <img
            src="<%= book.image_path %>"
            alt="Current Cover Image"
            style="max-width: 200px; height: auto;"
          />
        </div>
        <% } %>

        <div class="form-group">
          <label for="takeaways">Takeaways:</label>
          <textarea id="takeaways" name="takeaways"><%= book.takeaways %></textarea>
        </div>

        <div class="form-group">
          <label for="content">Content:</label>
          <textarea id="content" name="content"><%= book.content %></textarea>
        </div>

        <div class="button-container">
          <button type="submit" class="btn-primary">Update Book</button>
          <a href="/" class="btn-secondary">Back to Home</a>
        </div>
      </form>
    </div>
  </body>
</html>
